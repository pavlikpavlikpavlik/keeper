{% extends 'base.html.twig' %}

{% block title %}Транзакции{% endblock %}

{% block body %}
<div class="container text-center py-5">
    <div class="card-custom" style="max-width: 500px; margin: 0 auto;">
        <i class="fas fa-exchange-alt text-muted" style="font-size: 4rem;"></i>
        <h1 class="mt-3">💸 Транзакции</h1>
        <p class="text-muted mb-4">Управление вашими финансовыми операциями</p>
        
        <div class="d-grid gap-2">
            <a href="{{ path('app_transaction_new') }}" class="btn btn-smooth btn-primary-smooth">
                <i class="fas fa-plus"></i> Создать транзакцию
            </a>
            <a href="{{ path('app_transaction_index') }}" class="btn btn-smooth" style="background: #f2f2f7; color: #1d1d1f;">
                <i class="fas fa-list"></i> Все транзакции
            </a>
            <a href="{{ path('dashboard') }}" class="btn btn-smooth" style="background: #f2f2f7; color: #1d1d1f;">
                <i class="fas fa-chart-pie"></i> На дашборд
            </a>
        </div>
    </div>
</div>
{% endblock %}