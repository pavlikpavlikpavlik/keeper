{% extends 'base.html.twig' %}

{% block title %}Редактировать транзакцию{% endblock %}

{% block body %}
<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>✏️ Редактировать транзакцию</h1>
        <a href="{{ path('app_transaction_index') }}" class="btn btn-smooth" style="background: #f2f2f7; color: #1d1d1f;">
            <i class="fas fa-arrow-left"></i> Назад к списку
        </a>
    </div>

    <div class="card-custom">
        {{ include('transaction/_form.html.twig', {'button_label': 'Обновить'}) }}
    </div>

    <div class="mt-3">
        {{ include('transaction/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}