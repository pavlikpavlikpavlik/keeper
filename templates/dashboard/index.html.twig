{% extends 'base.html.twig' %}

{% block title %}Дашборд{% endblock %}

{% block body %}
<div class="container">
    <h1 class="mb-4">💰 Финансовый дашборд</h1>

    <!-- Статистика -->
    <div class="row mt-4">
        <div class="col-md-3">
            <div class="stat-card">
                <h6>Доходы</h6>
                <h3>{{ income_amount|number_format(2, '.', ' ') }} ₽</h3>
                {% if income_amount == 0 %}<small>Нет данных</small>{% endif %}
            </div>
        </div>
        <div class="col-md-3">
            <div class="stat-card">
                <h6>Расходы</h6>
                <h3>{{ expense_amount|number_format(2, '.', ' ') }} ₽</h3>
                {% if expense_amount == 0 %}<small>Нет данных</small>{% endif %}
            </div>
        </div>
        <div class="col-md-3">
            <div class="stat-card">
                <h6>Баланс</h6>
                <h3 class="{{ (income_amount - expense_amount) >= 0 ? 'text-balance-positive' : 'text-balance-negative' }}">
                    {{ (income_amount - expense_amount)|number_format(2, '.', ' ') }} ₽
                </h3>
            </div>
        </div>
        <div class="col-md-3">
            <div class="stat-card">
                <h6>Всего транзакций</h6>
                <h3>{{ recent_transactions|length }}</h3>
            </div>
        </div>
    </div>

    <!-- Быстрые действия -->
    <div class="row mt-4">
        <div class="col-md-6">
            <div class="card-custom">
                <h5 class="mb-3">🚀 Быстрые действия</h5>
                <div class="d-grid gap-2">
                    <a href="{{ path('app_transaction_new') }}" class="btn btn-smooth btn-primary-smooth">
                        <i class="fas fa-plus"></i> Добавить транзакцию
                    </a>
                    <a href="{{ path('app_transaction_index') }}" class="btn btn-smooth" style="background: #f2f2f7; color: #1d1d1f;">
                        <i class="fas fa-chart-bar"></i> Все транзакции
                    </a>
                    <a href="{{ path('app_category_index') }}" class="btn btn-smooth" style="background: #f2f2f7; color: #1d1d1f;">
                        <i class="fas fa-tags"></i> Управление категориями
                    </a>
                </div>
            </div>
        </div>
        
        <div class="col-md-6">
            <div class="card-custom">
                <h5 class="mb-3">📋 Последние транзакции</h5>
                <div class="transaction-list">
                    {% if recent_transactions %}
                        {% for transaction in recent_transactions %}
                            <div class="d-flex justify-content-between align-items-center py-3 border-bottom" style="border-color: #f2f2f7 !important;">
                                <div class="d-flex align-items-center">
                                    <div class="rounded-circle d-flex align-items-center justify-content-center me-3" 
                                         style="width: 40px; height: 40px; background: {{ transaction.category.color ?? '#007aff' }}; color: white;">
                                        <i class="fas fa-{{ transaction.amount < 0 ? 'arrow-down' : 'arrow-up' }}"></i>
                                    </div>
                                    <div>
                                        <strong class="d-block">{{ transaction.category.name }}</strong>
                                        <small class="text-muted">{{ transaction.description|default('Без описания') }}</small>
                                    </div>
                                </div>
                                <div class="text-{{ transaction.amount < 0 ? 'money-expense' : 'money-income' }} fw-bold">
                                    {{ transaction.amount }} BYN
                                </div>
                            </div>
                        {% endfor %}
                    {% else %}
                        <div class="text-center py-4">
                            <i class="fas fa-receipt text-muted" style="font-size: 3rem;"></i>
                            <p class="text-muted mt-2">Нет транзакций</p>
                            <a href="{{ path('app_transaction_new') }}" class="btn btn-smooth btn-primary-smooth mt-2">
                                Создать первую транзакцию
                            </a>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}